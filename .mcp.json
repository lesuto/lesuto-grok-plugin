{
  "mcpServers": {
    "lesuto": {
      "command": "node",
      "args": ["${GROK_PLUGIN_ROOT}/server.mjs"],
      "env": {
        "LESUTO_API_URL": "https://api.lesuto.com",
        "LESUTO_AGENT_KEY": "${LESUTO_AGENT_KEY}",
        "LESUTO_CHANNEL_TOKEN": "${LESUTO_CHANNEL_TOKEN}"
      }
    }
  }
}
