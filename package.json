{
  "name": "@lesuto/grok-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "Lesuto Grok MCP: channel-scoped admin GraphQL plus Lesuto Connect tools.",
  "type": "module",
  "bin": {
    "lesuto-grok": "./server.mjs"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test --test-concurrency=1 graphql.test.mjs protocol.test.mjs tools-contract.test.mjs public-contract.test.mjs denied-ops.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
